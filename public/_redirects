# The canonical map URL `/map/<corpus>/<book>/<location>/<lang>` is now served by
# the prerendered Astro page `src/pages/map/[corpus]/[book]/[location]/[lang].astro`,
# which reads the verbatim standalone file from `public/maps/.../<lang>.html` and
# wraps it with the site header + per-language SEO (ADR 0004, revised 2026-06-05).
# The raw files remain directly available under `/maps/...` for embedding/portability.
# No redirect rule is needed here anymore.
